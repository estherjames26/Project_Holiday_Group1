# Holiday Planner — run: pip install -r requirements.txt
# Requires Google Chrome installed for Airbnb scraping (scrape_airbnb.py).

# App
streamlit>=1.32.0
streamlit-folium>=0.18.0

# Data & charts
pandas>=2.1.0
numpy>=1.26.0
plotly>=5.18.0

# Maps
folium>=0.15.0

# HTTP & scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
selenium>=4.15.0
webdriver-manager>=4.0.0

# Google APIs (Places, Geocoding)
googlemaps>=4.10.0

# Database
sqlalchemy>=2.0.0

# Config
python-dotenv>=1.0.0

# Optional AI summary (api_openai.py — works without it via fallback text)
openai>=1.12.0

# Dev / tests
pytest>=8.0.0
